# 단일 도메인 통합 + 랜딩 + 사전등록 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** flori web 레포를 `flori.ai.kr` 단일 도메인으로 만들고(루트 인증 분기), hazel 스토어프론트를 flori 제품 랜딩으로 교체하며, 사전등록(waitlist) 기능을 web 폼 + api(BFF) 엔드포인트로 추가한다.

**Architecture:** middleware가 `/`를 인증 쿠키 유무로 분기(있으면 `/admin`, 없으면 랜딩). 랜딩은 기존 `(public)/` 그룹을 flori 콘텐츠로 재작성(어드민 cool-rose 화이트 팔레트). 사전등록은 공개 폼 → web Server Action(`apiFetch`, JWT 불필요) → Kotlin BFF 공개 엔드포인트 → `waitlist_registrations` 테이블. 100명 마감/카카오 오픈채팅 유도.

**Tech Stack:** Next.js 16 / React 19 / TS / Tailwind v4 / Zod 4 / Vitest (web) · Kotlin / Spring Boot / Spring Data JPA / Spring Security / JUnit5 + MockMvc + Zonky embedded PG (api)

**워크트리:**
- web: `/Users/hansangho/Desktop/flori-ai/flori-web-session2` (브랜치 `feature/session2-unified-domain`)
- api: `/Users/hansangho/Desktop/flori-ai/flori-api-session2` (브랜치 `feature/session2-unified-domain`)

**참조:**
- 설계 스펙: `docs/superpowers/specs/26-06-09-unified-domain-landing-waitlist-design.md`
- **확정 랜딩 시안(마크업·스타일 SSOT)**: `docs/superpowers/specs/26-06-09-landing-mockup.html` — 랜딩 섹션 JSX/클래스는 이 시안을 그대로 포팅한다.

**순서:** Part A(api 백엔드) → Part B(web). api가 먼저 준비되어야 web 통합이 매끄럽다. DB 마이그레이션 SQL은 만들되 **적용은 사용자 허락 후**(`ddl-auto: validate`라 자동 생성 안 됨).

---

## Part A — api (Kotlin BFF) · 워크트리 `flori-api-session2`

> 모든 `Run:` 명령은 api 워크트리에서 실행: `cd /Users/hansangho/Desktop/flori-ai/flori-api-session2`
> 테스트 러너: `./gradlew test`. 단일 클래스: `./gradlew test --tests "kr.ai.flori.waitlist.*"`

### Task A1: 마이그레이션 SQL + DDL 반영

**Files:**
- Create: `docs/sql/migration/26-06-10-waitlist.sql`
- Modify: `docs/sql/all-tables-ddl.sql` (waitlist 테이블 DDL 추가)

- [ ] **Step 1: 마이그레이션 SQL 작성**

`docs/sql/migration/26-06-10-waitlist.sql`:
```sql
-- 사전등록(waitlist) — 출시 전 선착순 100명 모집. 공개 폼(가게명+전화번호), 로그인 불필요.
-- 전화번호는 정규화(숫자만)하여 UNIQUE로 중복 등록 방지. 인증/테넌시와 무관한 독립 테이블.
START TRANSACTION;

CREATE TABLE waitlist_registrations (
    id          BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    shop_name   VARCHAR(50)  NOT NULL,
    phone       VARCHAR(20)  NOT NULL UNIQUE,
    created_at  TIMESTAMPTZ  NOT NULL DEFAULT now()
);

COMMIT;
```

- [ ] **Step 2: all-tables-ddl.sql 에 동일 DDL 추가**

`docs/sql/all-tables-ddl.sql` 끝에 위 `CREATE TABLE waitlist_registrations (...)` 블록을 (트랜잭션 래퍼 없이) 추가한다. 파일 내 기존 테이블 정의 스타일(컬럼 정렬)에 맞춘다.

- [ ] **Step 3: 커밋**

```bash
cd /Users/hansangho/Desktop/flori-ai/flori-api-session2
git add docs/sql/migration/26-06-10-waitlist.sql docs/sql/all-tables-ddl.sql
git commit -m "feat(api): waitlist_registrations 마이그레이션 SQL"
```

> ⚠️ 이 SQL은 **아직 DB에 적용하지 않는다.** Task A4 통합테스트는 Zonky 임베디드 PG에서 엔티티 기반으로 스키마를 생성하므로 실DB 불필요. 운영 DB 적용은 PR 후 사용자 승인하에.

---

### Task A2: 엔티티 + 리포지토리

**Files:**
- Create: `src/main/kotlin/kr/ai/flori/waitlist/entity/WaitlistRegistration.kt`
- Create: `src/main/kotlin/kr/ai/flori/waitlist/repository/WaitlistRegistrationRepository.kt`

- [ ] **Step 1: 엔티티 작성**

`src/main/kotlin/kr/ai/flori/waitlist/entity/WaitlistRegistration.kt`:
```kotlin
package kr.ai.flori.waitlist.entity

import jakarta.persistence.Column
import jakarta.persistence.Entity
import jakarta.persistence.GeneratedValue
import jakarta.persistence.GenerationType
import jakarta.persistence.Id
import jakarta.persistence.Table
import kr.ai.flori.common.entity.BaseCreatedEntity

/**
 * 사전등록 1건. 인증/테넌시와 무관한 공개 모집 데이터.
 * phone은 정규화(숫자만)하여 저장하며 UNIQUE로 중복 등록을 막는다.
 */
@Entity
@Table(name = "waitlist_registrations")
class WaitlistRegistration(
    @Column(name = "shop_name", nullable = false)
    var shopName: String,
    @Column(name = "phone", nullable = false, unique = true)
    var phone: String,
) : BaseCreatedEntity() {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    @Column(name = "id")
    var id: Long? = null
}
```

- [ ] **Step 2: 리포지토리 작성**

`src/main/kotlin/kr/ai/flori/waitlist/repository/WaitlistRegistrationRepository.kt`:
```kotlin
package kr.ai.flori.waitlist.repository

import kr.ai.flori.waitlist.entity.WaitlistRegistration
import org.springframework.data.jpa.repository.JpaRepository

interface WaitlistRegistrationRepository : JpaRepository<WaitlistRegistration, Long> {
    fun existsByPhone(phone: String): Boolean
}
```
(전체 카운트는 `JpaRepository.count()` 사용.)

- [ ] **Step 3: 컴파일 확인**

Run: `cd /Users/hansangho/Desktop/flori-ai/flori-api-session2 && ./gradlew compileKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 4: 커밋**

```bash
git add src/main/kotlin/kr/ai/flori/waitlist/entity/WaitlistRegistration.kt src/main/kotlin/kr/ai/flori/waitlist/repository/WaitlistRegistrationRepository.kt
git commit -m "feat(api): waitlist 엔티티·리포지토리"
```

---

### Task A3: 에러코드 + DTO + 서비스 (TDD)

**Files:**
- Create: `src/main/kotlin/kr/ai/flori/waitlist/error/WaitlistErrorCode.kt`
- Create: `src/main/kotlin/kr/ai/flori/waitlist/dto/WaitlistDtos.kt`
- Create: `src/main/kotlin/kr/ai/flori/waitlist/service/WaitlistService.kt`
- Test: `src/test/kotlin/kr/ai/flori/waitlist/service/WaitlistServiceIntegrationTest.kt`

- [ ] **Step 1: 에러코드 작성**

`src/main/kotlin/kr/ai/flori/waitlist/error/WaitlistErrorCode.kt`:
```kotlin
package kr.ai.flori.waitlist.error

import kr.ai.flori.common.error.ErrorCode
import org.springframework.http.HttpStatus

/** 사전등록 도메인 에러 코드. */
enum class WaitlistErrorCode(
    override val code: String,
    override val status: HttpStatus,
    override val defaultMessage: String,
) : ErrorCode {
    ALREADY_REGISTERED("E-WL-001", HttpStatus.CONFLICT, "이미 사전등록된 번호예요"),
    CLOSED("E-WL-002", HttpStatus.CONFLICT, "사전등록이 마감되었어요 (선착순 100명)"),
}
```

- [ ] **Step 2: DTO 작성**

`src/main/kotlin/kr/ai/flori/waitlist/dto/WaitlistDtos.kt`:
```kotlin
package kr.ai.flori.waitlist.dto

import jakarta.validation.constraints.NotBlank
import jakarta.validation.constraints.Pattern
import jakarta.validation.constraints.Size

/** 사전등록 요청. phone은 하이픈 허용(서버에서 정규화). */
data class WaitlistRegisterRequest(
    @field:NotBlank(message = "가게명을 입력해 주세요")
    @field:Size(max = 50, message = "가게명은 50자 이내여야 합니다")
    val shopName: String,
    @field:Pattern(regexp = "^01[016789]-?\\d{3,4}-?\\d{4}$", message = "전화번호 형식이 올바르지 않습니다")
    val phone: String,
)

/** 사전등록 응답(등록 직후). */
data class WaitlistRegisterResponse(
    val count: Long,
    val capacity: Int,
    val closed: Boolean,
)

/** 카운트 조회 응답. */
data class WaitlistCountResponse(
    val count: Long,
    val capacity: Int,
    val closed: Boolean,
)
```

- [ ] **Step 3: 실패 테스트 작성**

`src/test/kotlin/kr/ai/flori/waitlist/service/WaitlistServiceIntegrationTest.kt`:
```kotlin
package kr.ai.flori.waitlist.service

import io.zonky.test.db.AutoConfigureEmbeddedDatabase
import io.zonky.test.db.AutoConfigureEmbeddedDatabase.DatabaseProvider
import kr.ai.flori.common.error.AppException
import kr.ai.flori.waitlist.dto.WaitlistRegisterRequest
import kr.ai.flori.waitlist.error.WaitlistErrorCode
import kr.ai.flori.waitlist.repository.WaitlistRegistrationRepository
import org.assertj.core.api.Assertions.assertThat
import org.junit.jupiter.api.Test
import org.junit.jupiter.api.assertThrows
import org.springframework.beans.factory.annotation.Autowired
import org.springframework.boot.test.context.SpringBootTest

@AutoConfigureEmbeddedDatabase(provider = DatabaseProvider.ZONKY)
@SpringBootTest
class WaitlistServiceIntegrationTest {
    @Autowired private lateinit var service: WaitlistService
    @Autowired private lateinit var repository: WaitlistRegistrationRepository

    @Test
    fun `등록하면 카운트가 증가하고 전화번호는 정규화되어 저장된다`() {
        val res = service.register(WaitlistRegisterRequest(shopName = "헤이즐", phone = "010-1234-5678"))
        assertThat(res.count).isEqualTo(1L)
        assertThat(res.closed).isFalse()
        assertThat(repository.existsByPhone("01012345678")).isTrue()
    }

    @Test
    fun `같은 번호(형식만 다름)로 재등록하면 ALREADY_REGISTERED`() {
        service.register(WaitlistRegisterRequest(shopName = "A", phone = "01099998888"))
        val ex = assertThrows<AppException> {
            service.register(WaitlistRegisterRequest(shopName = "B", phone = "010-9999-8888"))
        }
        assertThat(ex.errorCode).isEqualTo(WaitlistErrorCode.ALREADY_REGISTERED)
    }

    @Test
    fun `정원이 차면 CLOSED를 던진다`() {
        // capacity=2로 가정한 별도 인스턴스 대신, 서비스 상수 CAPACITY 경계 검증은 count 기반.
        // 여기서는 2건만 채우고 CAPACITY를 넘기지 않으므로 closed=false 만 확인.
        service.register(WaitlistRegisterRequest(shopName = "A", phone = "01000000001"))
        val res = service.register(WaitlistRegisterRequest(shopName = "B", phone = "01000000002"))
        assertThat(res.closed).isFalse()
    }
}
```

- [ ] **Step 4: 테스트 실패 확인**

Run: `./gradlew test --tests "kr.ai.flori.waitlist.service.WaitlistServiceIntegrationTest"`
Expected: FAIL (WaitlistService 미존재 → 컴파일 에러)

- [ ] **Step 5: 서비스 구현**

`src/main/kotlin/kr/ai/flori/waitlist/service/WaitlistService.kt`:
```kotlin
package kr.ai.flori.waitlist.service

import kr.ai.flori.common.error.AppException
import kr.ai.flori.waitlist.dto.WaitlistCountResponse
import kr.ai.flori.waitlist.dto.WaitlistRegisterRequest
import kr.ai.flori.waitlist.dto.WaitlistRegisterResponse
import kr.ai.flori.waitlist.entity.WaitlistRegistration
import kr.ai.flori.waitlist.error.WaitlistErrorCode
import kr.ai.flori.waitlist.repository.WaitlistRegistrationRepository
import org.springframework.dao.DataIntegrityViolationException
import org.springframework.stereotype.Service
import org.springframework.transaction.annotation.Transactional

/**
 * 사전등록 서비스. 인증/테넌시 없음(공개 모집).
 * 선착순 [CAPACITY]명까지. phone은 숫자만 남겨 정규화·중복검사.
 */
@Service
class WaitlistService(
    private val repository: WaitlistRegistrationRepository,
) {
    @Transactional
    fun register(request: WaitlistRegisterRequest): WaitlistRegisterResponse {
        if (repository.count() >= CAPACITY) {
            throw AppException(WaitlistErrorCode.CLOSED)
        }
        val phone = normalizePhone(request.phone)
        if (repository.existsByPhone(phone)) {
            throw AppException(WaitlistErrorCode.ALREADY_REGISTERED)
        }
        try {
            repository.save(WaitlistRegistration(shopName = request.shopName.trim(), phone = phone))
        } catch (e: DataIntegrityViolationException) {
            throw AppException(WaitlistErrorCode.ALREADY_REGISTERED, cause = e)
        }
        val count = repository.count()
        return WaitlistRegisterResponse(count = count, capacity = CAPACITY, closed = count >= CAPACITY)
    }

    @Transactional(readOnly = true)
    fun count(): WaitlistCountResponse {
        val count = repository.count()
        return WaitlistCountResponse(count = count, capacity = CAPACITY, closed = count >= CAPACITY)
    }

    private fun normalizePhone(raw: String): String = raw.filter { it.isDigit() }

    companion object {
        const val CAPACITY = 100
    }
}
```

- [ ] **Step 6: 테스트 통과 확인**

Run: `./gradlew test --tests "kr.ai.flori.waitlist.service.WaitlistServiceIntegrationTest"`
Expected: PASS (3 tests)

- [ ] **Step 7: 커밋**

```bash
git add src/main/kotlin/kr/ai/flori/waitlist/error src/main/kotlin/kr/ai/flori/waitlist/dto src/main/kotlin/kr/ai/flori/waitlist/service src/test/kotlin/kr/ai/flori/waitlist
git commit -m "feat(api): waitlist 서비스(등록·카운트·정규화·중복·마감) + 통합테스트"
```

---

### Task A4: 컨트롤러 + 공개 라우트 등록 (MockMvc 테스트)

**Files:**
- Create: `src/main/kotlin/kr/ai/flori/waitlist/controller/WaitlistController.kt`
- Modify: `src/main/kotlin/kr/ai/flori/common/security/SecurityConfig.kt` (permitAll)
- Test: `src/test/kotlin/kr/ai/flori/waitlist/controller/WaitlistControllerTest.kt`

- [ ] **Step 1: 컨트롤러 작성**

`src/main/kotlin/kr/ai/flori/waitlist/controller/WaitlistController.kt`:
```kotlin
package kr.ai.flori.waitlist.controller

import jakarta.validation.Valid
import kr.ai.flori.waitlist.dto.WaitlistCountResponse
import kr.ai.flori.waitlist.dto.WaitlistRegisterRequest
import kr.ai.flori.waitlist.dto.WaitlistRegisterResponse
import kr.ai.flori.waitlist.service.WaitlistService
import org.springframework.http.HttpStatus
import org.springframework.web.bind.annotation.GetMapping
import org.springframework.web.bind.annotation.PostMapping
import org.springframework.web.bind.annotation.RequestBody
import org.springframework.web.bind.annotation.RequestMapping
import org.springframework.web.bind.annotation.ResponseStatus
import org.springframework.web.bind.annotation.RestController

/** 사전등록(공개). 인증 불필요 — SecurityConfig에서 permitAll. */
@RestController
@RequestMapping("/waitlist")
class WaitlistController(
    private val service: WaitlistService,
) {
    @PostMapping
    @ResponseStatus(HttpStatus.CREATED)
    fun register(
        @Valid @RequestBody request: WaitlistRegisterRequest,
    ): WaitlistRegisterResponse = service.register(request)

    @GetMapping("/count")
    fun count(): WaitlistCountResponse = service.count()
}
```

- [ ] **Step 2: SecurityConfig 공개 경로 추가**

`SecurityConfig.kt`의 `authorizeHttpRequests { ... }` 블록에서 `authorize("/auth/nickname/check", permitAll)` 줄 **다음**에 추가:
```kotlin
                authorize("/waitlist", permitAll)
                authorize("/waitlist/count", permitAll)
```

- [ ] **Step 3: 컨트롤러 테스트 작성**

`src/test/kotlin/kr/ai/flori/waitlist/controller/WaitlistControllerTest.kt`:
```kotlin
package kr.ai.flori.waitlist.controller

import com.fasterxml.jackson.databind.ObjectMapper
import kr.ai.flori.common.error.DiscordErrorReporter
import kr.ai.flori.waitlist.dto.WaitlistCountResponse
import kr.ai.flori.waitlist.dto.WaitlistRegisterRequest
import kr.ai.flori.waitlist.dto.WaitlistRegisterResponse
import kr.ai.flori.waitlist.service.WaitlistService
import org.junit.jupiter.api.Test
import org.mockito.kotlin.any
import org.mockito.kotlin.whenever
import org.springframework.beans.factory.annotation.Autowired
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc
import org.springframework.boot.test.autoconfigure.web.servlet.WebMvcTest
import org.springframework.context.annotation.Import
import org.springframework.test.context.bean.override.mockito.MockitoBean
import org.springframework.test.web.servlet.MockMvc
import org.springframework.test.web.servlet.get
import org.springframework.test.web.servlet.post

@WebMvcTest(WaitlistController::class)
@AutoConfigureMockMvc(addFilters = false)
@Import(DiscordErrorReporter::class)
class WaitlistControllerTest {
    @Autowired lateinit var mockMvc: MockMvc
    @Autowired lateinit var objectMapper: ObjectMapper
    @MockitoBean lateinit var service: WaitlistService

    @Test
    fun `POST waitlist 는 201과 카운트를 반환한다`() {
        whenever(service.register(any())).thenReturn(WaitlistRegisterResponse(count = 1, capacity = 100, closed = false))
        val body = objectMapper.writeValueAsString(WaitlistRegisterRequest("헤이즐", "010-1234-5678"))
        mockMvc.post("/waitlist") {
            contentType = org.springframework.http.MediaType.APPLICATION_JSON
            content = body
        }.andExpect {
            status { isCreated() }
            jsonPath("$.count") { value(1) }
            jsonPath("$.capacity") { value(100) }
        }
    }

    @Test
    fun `GET waitlist count 는 카운트를 반환한다`() {
        whenever(service.count()).thenReturn(WaitlistCountResponse(count = 24, capacity = 100, closed = false))
        mockMvc.get("/waitlist/count").andExpect {
            status { isOk() }
            jsonPath("$.count") { value(24) }
            jsonPath("$.closed") { value(false) }
        }
    }

    @Test
    fun `POST waitlist 가게명 누락 시 400`() {
        val body = objectMapper.writeValueAsString(WaitlistRegisterRequest("", "010-1234-5678"))
        mockMvc.post("/waitlist") {
            contentType = org.springframework.http.MediaType.APPLICATION_JSON
            content = body
        }.andExpect { status { isBadRequest() } }
    }
}
```

> 주: `@MockitoBean`(Spring Boot 3.4+)·`org.mockito.kotlin` 사용. 만약 프로젝트가 구버전이면 기존 테스트에서 쓰는 모킹 방식(`@MockBean`)으로 맞춘다 — `src/test`의 다른 `@WebMvcTest`를 grep해 동일 import를 사용할 것.

- [ ] **Step 4: 테스트 실행**

Run: `./gradlew test --tests "kr.ai.flori.waitlist.controller.WaitlistControllerTest"`
Expected: PASS (3 tests). 실패 시 모킹 어노테이션 import를 기존 패턴에 맞춰 수정.

- [ ] **Step 5: 전체 빌드·테스트**

Run: `./gradlew build`
Expected: BUILD SUCCESSFUL

- [ ] **Step 6: 커밋**

```bash
git add src/main/kotlin/kr/ai/flori/waitlist/controller src/main/kotlin/kr/ai/flori/common/security/SecurityConfig.kt src/test/kotlin/kr/ai/flori/waitlist/controller
git commit -m "feat(api): waitlist 공개 컨트롤러(POST /waitlist, GET /waitlist/count) + permitAll"
```

---

## Part B — web · 워크트리 `flori-web-session2`

> 모든 `Run:` 명령은 web 워크트리에서: `cd /Users/hansangho/Desktop/flori-ai/flori-web-session2`
> 테스트: `npm test`. 단일: `npm test -- src/lib/__tests__/middleware-root.test.ts`

### Task B1: 미들웨어 루트 분기 (TDD)

`/` 분기 로직을 순수 함수로 분리해 단위 테스트한 뒤 미들웨어에 연결한다.

**Files:**
- Modify: `src/middleware.ts`
- Create: `src/lib/middleware-routing.ts`
- Test: `src/lib/__tests__/middleware-routing.test.ts`

- [ ] **Step 1: 실패 테스트 작성**

`src/lib/__tests__/middleware-routing.test.ts`:
```ts
import {describe, expect, it} from 'vitest';
import {rootRedirectTarget} from '@/lib/middleware-routing';

describe('rootRedirectTarget', () => {
  it('루트가 아니면 null', () => {
    expect(rootRedirectTarget('/login', {hasAccess: false, hasRefresh: false})).toBeNull();
    expect(rootRedirectTarget('/admin', {hasAccess: true, hasRefresh: true})).toBeNull();
  });
  it('루트 + 인증 쿠키 있으면 /admin', () => {
    expect(rootRedirectTarget('/', {hasAccess: true, hasRefresh: false})).toBe('/admin');
    expect(rootRedirectTarget('/', {hasAccess: false, hasRefresh: true})).toBe('/admin');
  });
  it('루트 + 쿠키 없으면 null(랜딩 렌더)', () => {
    expect(rootRedirectTarget('/', {hasAccess: false, hasRefresh: false})).toBeNull();
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm test -- src/lib/__tests__/middleware-routing.test.ts`
Expected: FAIL (모듈 없음)

- [ ] **Step 3: 순수 함수 구현**

`src/lib/middleware-routing.ts`:
```ts
// 미들웨어 루트 분기 판정(Edge 안전 — next/headers 의존 없음).
export interface AuthCookiePresence {
  hasAccess: boolean;
  hasRefresh: boolean;
}

/**
 * 루트(`/`) 요청에서 리다이렉트 대상 경로를 반환한다.
 * - `/` 이고 인증 쿠키(access 또는 refresh)가 존재하면 `/admin`.
 * - 그 외(루트 아님 / 쿠키 없음)는 null(분기 없음).
 * 쿠키 "존재"만 본다 — 유효성 검증은 /admin 진입 가드가 수행한다.
 */
export function rootRedirectTarget(pathname: string, cookies: AuthCookiePresence): string | null {
  if (pathname !== '/') return null;
  if (cookies.hasAccess || cookies.hasRefresh) return '/admin';
  return null;
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test -- src/lib/__tests__/middleware-routing.test.ts`
Expected: PASS

- [ ] **Step 5: 미들웨어에 연결**

`src/middleware.ts`의 `export async function middleware` 안, `const { pathname } = request.nextUrl` 다음에 추가:
```ts
  // 루트 분기: 인증 쿠키 있으면 /admin, 없으면 랜딩 렌더
  const rootTarget = rootRedirectTarget(pathname, {
    hasAccess: request.cookies.has(ACCESS_COOKIE),
    hasRefresh: request.cookies.has(REFRESH_COOKIE),
  });
  if (rootTarget) {
    return NextResponse.redirect(new URL(rootTarget, request.url));
  }
```
그리고 파일 상단 import에 추가:
```ts
import {rootRedirectTarget} from '@/lib/middleware-routing';
```
(기존 `if (!pathname.startsWith('/admin') && !pathname.startsWith('/console')) return NextResponse.next()` 가드는 그대로 둔다 — 루트는 위에서 이미 처리되어 통과 시 랜딩 렌더.)

- [ ] **Step 6: 전체 테스트·린트**

Run: `npm test -- src/lib/__tests__/middleware-routing.test.ts && npm run lint`
Expected: PASS / no errors

- [ ] **Step 7: 커밋**

```bash
git add src/middleware.ts src/lib/middleware-routing.ts src/lib/__tests__/middleware-routing.test.ts
git commit -m "feat(web): 루트(/) 인증 분기 — 쿠키 있으면 /admin redirect"
```

---

### Task B2: 사전등록 검증 스키마 + Server Action (TDD)

**Files:**
- Modify: `src/lib/validations.ts` (waitlistSchema 추가)
- Create: `src/lib/actions/waitlist.ts`
- Test: `src/lib/__tests__/waitlist-validation.test.ts`

- [ ] **Step 1: 실패 테스트 작성**

`src/lib/__tests__/waitlist-validation.test.ts`:
```ts
import {describe, expect, it} from 'vitest';
import {waitlistSchema} from '@/lib/validations';

describe('waitlistSchema', () => {
  it('가게명+전화번호 정상', () => {
    const r = waitlistSchema.safeParse({shop_name: '헤이즐', phone: '010-1234-5678'});
    expect(r.success).toBe(true);
  });
  it('가게명 빈값 거부', () => {
    expect(waitlistSchema.safeParse({shop_name: '', phone: '010-1234-5678'}).success).toBe(false);
  });
  it('전화번호 형식 오류 거부', () => {
    expect(waitlistSchema.safeParse({shop_name: 'A', phone: '123'}).success).toBe(false);
  });
  it('가게명 50자 초과 거부', () => {
    expect(waitlistSchema.safeParse({shop_name: 'ㄱ'.repeat(51), phone: '01012345678'}).success).toBe(false);
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm test -- src/lib/__tests__/waitlist-validation.test.ts`
Expected: FAIL (waitlistSchema 미존재)

- [ ] **Step 3: 스키마 추가**

`src/lib/validations.ts` 끝에 추가(기존 `phoneSchema` 재사용):
```ts
// 사전등록(waitlist) — 가게명 + 전화번호
export const waitlistSchema = z.object({
  shop_name: z.string().min(1, '가게명을 입력해주세요').max(50, '가게명은 50자 이내여야 합니다'),
  phone: phoneSchema,
});
export type WaitlistInput = z.infer<typeof waitlistSchema>;
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test -- src/lib/__tests__/waitlist-validation.test.ts`
Expected: PASS

- [ ] **Step 5: Server Action 구현**

`src/lib/actions/waitlist.ts`:
```ts
'use server';

import {apiFetch} from '@/lib/api/client';
import {AppError, ErrorCode, withErrorLogging} from '@/lib/errors';
import {waitlistSchema} from '@/lib/validations';

// ─── 사전등록 Server Actions ─────────────────────────────────
// BFF WaitlistController (/waitlist) — 공개 엔드포인트(JWT 불필요).
// apiFetch는 토큰이 없으면 Authorization 없이 호출하며, 공개 엔드포인트는 401을 내지 않는다.

export interface WaitlistCount {
  count: number;
  capacity: number;
  closed: boolean;
}

// BFF: GET /waitlist/count
async function _getWaitlistCount(): Promise<WaitlistCount> {
  return apiFetch<WaitlistCount>('/waitlist/count');
}
export const getWaitlistCount = withErrorLogging('getWaitlistCount', _getWaitlistCount);

// BFF: POST /waitlist
async function _submitWaitlist(input: {shopName: string; phone: string}): Promise<WaitlistCount> {
  const parsed = waitlistSchema.safeParse({shop_name: input.shopName, phone: input.phone});
  if (!parsed.success) {
    throw new AppError(ErrorCode.VALIDATION, parsed.error.issues[0]?.message ?? '입력값을 확인해 주세요');
  }
  return apiFetch<WaitlistCount>('/waitlist', {
    method: 'POST',
    body: JSON.stringify({shopName: parsed.data.shop_name, phone: parsed.data.phone}),
  });
}
export const submitWaitlist = withErrorLogging('submitWaitlist', _submitWaitlist);
```

- [ ] **Step 6: 린트·테스트**

Run: `npm test -- src/lib/__tests__/waitlist-validation.test.ts && npm run lint`
Expected: PASS / no errors

- [ ] **Step 7: 커밋**

```bash
git add src/lib/validations.ts src/lib/actions/waitlist.ts src/lib/__tests__/waitlist-validation.test.ts
git commit -m "feat(web): 사전등록 검증 스키마 + Server Action(getWaitlistCount/submitWaitlist)"
```

---

### Task B3: 랜딩 콘텐츠 데이터 + env 추가

**Files:**
- Create: `src/lib/landing-content.ts`
- Modify: `src/lib/env.ts` (NEXT_PUBLIC_KAKAO_OPENCHAT_URL 추가)

- [ ] **Step 1: 랜딩 콘텐츠 상수 작성**

`src/lib/landing-content.ts` (기능 카드·FAQ를 데이터로 분리 — 시안 확정본 문구 사용):
```ts
// flori 랜딩 콘텐츠 SSOT(공개용). 시안: docs/superpowers/specs/26-06-09-landing-mockup.html
export interface LandingFeature {
  icon: string;       // 이모지(시안 기준). 추후 lucide 교체 가능
  title: string;
  body: string;
  bullets: string[];
}

export const LANDING_FEATURES: LandingFeature[] = [
  {
    icon: '📊',
    title: '오늘 번 돈, 쓴 돈이 한눈에',
    body: '현금·카드 매출과 재료비·고정비를 기록하면 월별·카테고리별로 자동 정리돼요. 외상(미수)도 따로 챙겨, 입금되면 한 번에 정산.',
    bullets: ['현금·카드·채널별 매출 통계', '미수금 관리 · 결제 완료 처리', '매달 나가는 고정비 자동 기록'],
  },
  {
    icon: '👥',
    title: '단골과 작품을 기억해요',
    body: '전화번호로 단골을 알아보고 구매 이력을 쌓아요. 완성한 꽃은 사진첩에 포트폴리오처럼 모아 고객과 연결.',
    bullets: ['전화번호 기반 고객 식별 · 등급', '작품 사진첩(카드당 10장) · 태그', '매출에 고객 연결 → 이력 자동 누적'],
  },
  {
    icon: '📅',
    title: '픽업·배송, 놓치지 않게',
    body: '예약을 등록하고 리마인더를 켜면 픽업·배송 전에 푸시로 알려드려요. 매일 아침 8시엔 그날 예약 요약까지.',
    bullets: ['픽업·배송 전 푸시 리마인더', '매일 오전 8시 그날 예약 요약', '예약 → 매출 바로 전환'],
  },
  {
    icon: '💬',
    title: '전국 사장님과 노하우를 나눠요',
    body: '실제 꽃집 사장님들만의 공간. 시세·거래처·운영 팁을 묻고 답해요. 사업자 인증을 한 번 하면 자유롭게.',
    bullets: ['사업자 인증 회원 전용', '카테고리별 글 · 대댓글 · 좋아요', '민감한 글은 비밀글로'],
  },
];

export interface LandingFaq {
  q: string;
  a: string;
}

export const LANDING_FAQS: LandingFaq[] = [
  {q: 'flori는 어떤 서비스인가요?', a: '꽃집 사장님을 위한 운영 서비스예요. 매출·지출·고객·예약·사진첩을 flori 하나로 기록하고, 전국 꽃집 사장님들과 소통할 수 있어요. 복잡한 POS 단말이나 설치 없이 바로 시작합니다.'},
  {q: '어떻게 시작하나요?', a: '카카오·네이버·구글 계정으로 로그인하면 끝이에요. 가게 정보만 간단히 입력하면 바로 매출을 기록할 수 있어요.'},
  {q: '비용이 드나요?', a: '출시 전 사전등록 선착순 100명에게는 첫 달 무료 혜택을 드려요. (이후 가격 정책은 확정되면 안내드릴게요.)'},
  {q: '외상(미수)은 어떻게 관리하나요?', a: '판매할 때 미수로 등록해 두면 "미수 중"으로 표시돼요. 나중에 입금되면 "결제 완료"로 바꿔 한 번에 정산합니다.'},
  {q: '예약 알림은 언제 오나요?', a: '예약에 리마인더를 설정하면 픽업·배송 시간 전에 푸시 알림이 와요. 매일 오전 8시엔 그날 예약 요약도 보내드려요. (설정에서 푸시 알림을 켜야 합니다.)'},
  {q: '커뮤니티는 왜 사업자 인증이 필요한가요?', a: '실제로 운영 중인 꽃집 사장님들만의 공간을 위해서예요. 사업자등록증으로 한 번 인증하면 이후 자유롭게 이용할 수 있어요.'},
];
```

- [ ] **Step 2: env 스키마에 카카오 오픈채팅 URL 추가**

`src/lib/env.ts`의 `serverEnvSchema` 객체 안 "선택: 기능별" 영역에 추가:
```ts
  // 사전등록 후 안내할 카카오톡 오픈채팅 링크(공개 URL). 미설정 시 버튼 비활성.
  NEXT_PUBLIC_KAKAO_OPENCHAT_URL: z.string().url().optional(),
```

- [ ] **Step 3: 빌드 영향 확인(타입)**

Run: `npx tsc --noEmit`
Expected: 새 파일 타입 에러 없음 (기존 에러는 무시 — 변경분만 확인)

- [ ] **Step 4: 커밋**

```bash
git add src/lib/landing-content.ts src/lib/env.ts
git commit -m "feat(web): 랜딩 콘텐츠 SSOT(기능·FAQ) + 카카오 오픈채팅 env"
```

---

### Task B4: 랜딩 팔레트(globals) + 레이아웃 메타데이터 교체

**Files:**
- Modify: `src/app/globals.css` (`.site-public` 변수 cool-rose 화이트로 갱신)
- Modify: `src/app/(public)/layout.tsx` (hazel SEO/JSON-LD 제거 → flori)

- [ ] **Step 1: `.site-public` 팔레트 변수 갱신**

`src/app/globals.css`에서 `.site-public` 스코프의 CSS 변수(`--site-paper`, `--site-ink`, `--site-accent` 등)를 시안 팔레트로 교체한다. 시안 `:root` 값 기준:
```css
.site-public {
  --site-paper: #FAFAF9;
  --site-paper-soft: #FFFFFF;
  --site-ink: #1C2024;
  --site-ink-soft: #5A6472;
  --site-muted: #9A9690;
  --site-line: #ECEAE6;
  --site-accent: #A85475;        /* dusty rose 포인트 */
  --site-accent-deep: #8E3F5F;
  --site-accent-soft: #F7E9EF;
  --site-slate: #8A929E;
}
```
> 기존 Sage & Wood legacy alias가 다른 곳에서 참조되면 깨지지 않게, 기존 변수명을 유지하되 값만 위로 덮어쓴다. 제거되는 hazel 컴포넌트(Task B6)에서만 쓰이던 변수는 함께 정리.

- [ ] **Step 2: layout.tsx 메타데이터 flori로 교체**

`src/app/(public)/layout.tsx`:
- `HAZEL_SEO`/`HAZEL_BUSINESS`/`HAZEL_LINKS` import 및 `buildJsonLd()`(FlowerStore) 제거.
- 메타데이터를 flori 제품으로:
```tsx
const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? 'https://flori.ai.kr';

export const metadata: Metadata = {
  metadataBase: new URL(SITE_URL),
  title: 'flori — 꽃집 사장님을 위한 운영 서비스',
  description: '매출·지출·고객·예약·사진첩까지. 꽃집 운영에 필요한 모든 기록을 flori 하나로.',
  openGraph: {
    title: 'flori — 꽃집 사장님을 위한 운영 서비스',
    description: '꽃집 운영에 필요한 모든 기록을 flori 하나로.',
    locale: 'ko_KR',
    type: 'website',
  },
};
```
- JSON-LD `<script>`는 제거(또는 `SoftwareApplication` 최소형으로). 폰트 변수(`cormorant`, `notoSerifKr`)와 `.site-public` 래퍼, `<PublicHeader/>`·`<PublicFooter/>` 구조는 유지(컴포넌트 내용은 Task B5/B6에서 교체).

- [ ] **Step 3: 린트·타입**

Run: `npm run lint`
Expected: hazel import 잔존 에러 없음(있으면 Task B6에서 정리되는 컴포넌트 참조이므로, B6와 함께 처리). layout.tsx 자체는 클린.

- [ ] **Step 4: 커밋**

```bash
git add src/app/globals.css "src/app/(public)/layout.tsx"
git commit -m "feat(web): 랜딩 팔레트 cool-rose 화이트 + 레이아웃 메타데이터 flori 교체"
```

---

### Task B5: 랜딩 섹션 컴포넌트 (시안 포팅)

확정 시안 `docs/superpowers/specs/26-06-09-landing-mockup.html`을 React 컴포넌트로 포팅한다. **마크업·클래스·스타일은 시안과 1:1**, 단 색은 인라인 hex 대신 `.site-public` CSS 변수를 쓰고, 데이터는 Task B3 상수를 map.

**Files (모두 Create):**
- `src/components/public/landing-header.tsx` — sticky 헤더(`flori.` + 앵커 + 로그인 `Link href="/login"`)
- `src/components/public/landing-hero.tsx` — Hero(칩·H1·서브카피·듀얼 CTA·소셜 안내·제품 미리보기 슬롯)
- `src/components/public/waitlist-section.tsx` — `'use client'` 사전등록(폼+카운트+카카오+마감). props: `initialCount: WaitlistCount`, `kakaoUrl?: string`
- `src/components/public/landing-features.tsx` — 교차 4행(`LANDING_FEATURES` map) + 보조 그리드
- `src/components/public/landing-faq.tsx` — `<details>` 토글 카드(`LANDING_FAQS` map)
- `src/components/public/landing-cta.tsx` — 다크 CTA 밴드
- `src/components/public/landing-footer.tsx` — `flori.` + `/policy/privacy`·`/policy/terms` 링크

- [ ] **Step 1: 정적 섹션 컴포넌트 작성 (header/hero/features/faq/cta/footer)**

각 컴포넌트는 시안의 해당 `<section>`/`<header>`/`<footer>` 마크업을 그대로 JSX로 옮긴다. 규칙:
- 인라인 `style="...hex..."` → 가능한 한 `style={{...}}`에 `var(--site-*)` 사용(팔레트 일원화). 레이아웃용 clamp/grid 인라인 스타일은 시안 값 유지.
- 텍스트/링크: 로그인 버튼·CTA는 `next/link`의 `<Link href="/login">`, 푸터 정책 링크는 `<Link href="/policy/privacy">`·`<Link href="/policy/terms">`, 앵커는 `<a href="#features">` 등 시안과 동일.
- features/faq는 Task B3 데이터 map:
```tsx
// landing-features.tsx — 교차 4행
import {LANDING_FEATURES} from '@/lib/landing-content';
// ...각 feature를 시안의 .row 구조로 렌더(짝수 인덱스는 이미지 좌측 order:-1). bullets는 ✓ 리스트.
```
```tsx
// landing-faq.tsx
import {LANDING_FAQS} from '@/lib/landing-content';
// ...LANDING_FAQS.map((f,i)=> <details key={f.q} open={i===0}> <summary>{f.q}</summary> <div className="ans">{f.a}</div> </details>)
```
- 제품 미리보기/기능 캡처 슬롯은 시안의 `.ph` 플레이스홀더 박스를 그대로 유지(에셋 추후 교체). `.ph`/`.browser`/`details` 등 시안 `<style>`의 규칙은 `globals.css`의 `.site-public` 스코프로 옮겨 추가한다(시안 `<style>` 블록 참고).

- [ ] **Step 2: waitlist-section.tsx (클라이언트) 작성**

```tsx
'use client';

import {useState} from 'react';
import {submitWaitlist, type WaitlistCount} from '@/lib/actions/waitlist';

export function WaitlistSection({initialCount, kakaoUrl}: {initialCount: WaitlistCount; kakaoUrl?: string}) {
  const [count, setCount] = useState(initialCount);
  const [shopName, setShopName] = useState('');
  const [phone, setPhone] = useState('');
  const [submitting, setSubmitting] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const [registered, setRegistered] = useState(false);

  const closed = count.closed;
  const pct = Math.min(100, Math.round((count.count / count.capacity) * 100));
  const remaining = Math.max(0, count.capacity - count.count);

  async function onSubmit(e: React.FormEvent) {
    e.preventDefault();
    setError(null);
    setSubmitting(true);
    try {
      const next = await submitWaitlist({shopName, phone});
      setCount(next);
      setRegistered(true);
    } catch (err) {
      setError(err instanceof Error ? err.message : '잠시 후 다시 시도해 주세요');
    } finally {
      setSubmitting(false);
    }
  }
  // 렌더: 시안의 #waitlist 섹션 구조.
  //  - 게이지: pct% width, "{count.count} / {capacity}명 등록 완료 · {remaining}자리 남음"
  //  - closed === true  → 시안의 "100명 마감 시" 카드(폼 숨김 + 카카오 CTA)
  //  - registered === true → STEP2 카카오 단계 강조("등록 완료! 마지막으로 오픈채팅 참여")
  //  - 그 외 → STEP1 폼(가게명/전화번호, value/onChange 바인딩, error 표시) + STEP2 카카오 안내
  //  - 카카오 버튼: kakaoUrl 있으면 <a href={kakaoUrl} target="_blank" rel="noopener noreferrer">, 없으면 disabled 스타일 + "오픈채팅 링크 준비 중"
  //  - 개인정보 고지: "입력하신 가게명·연락처는 사전등록 안내 연락에만 써요. 등록 시 [개인정보처리방침](/policy/privacy)에 동의하게 됩니다."
  return (/* 시안 #waitlist 마크업 — 위 상태로 분기 */ null);
}
```
> Step 2의 JSX 본문은 시안 `#waitlist` 섹션 마크업을 그대로 옮기고 위 분기/바인딩만 추가한다. 시안이 SSOT이므로 클래스·인라인 스타일을 동일하게 사용한다.

- [ ] **Step 3: 페이지 조립**

`src/app/(public)/page.tsx` 전체 교체:
```tsx
import {LandingHeader} from '@/components/public/landing-header';
import {LandingHero} from '@/components/public/landing-hero';
import {WaitlistSection} from '@/components/public/waitlist-section';
import {LandingFeatures} from '@/components/public/landing-features';
import {LandingFaq} from '@/components/public/landing-faq';
import {LandingCta} from '@/components/public/landing-cta';
import {LandingFooter} from '@/components/public/landing-footer';
import {getWaitlistCount, type WaitlistCount} from '@/lib/actions/waitlist';

export default async function HomePage() {
  let count: WaitlistCount = {count: 0, capacity: 100, closed: false};
  try {
    count = await getWaitlistCount();
  } catch {
    // BFF 미가용 시 0으로 폴백(랜딩은 렌더되어야 함)
  }
  const kakaoUrl = process.env.NEXT_PUBLIC_KAKAO_OPENCHAT_URL;
  return (
    <>
      <LandingHeader />
      <LandingHero />
      <WaitlistSection initialCount={count} kakaoUrl={kakaoUrl} />
      <LandingFeatures />
      <LandingFaq />
      <LandingCta />
      <LandingFooter />
    </>
  );
}
```
> `(public)/layout.tsx`가 이미 `<PublicHeader/>`·`<PublicFooter/>`를 렌더한다면, 중복을 피하기 위해 layout에서 헤더/푸터를 제거하고 page의 `LandingHeader`/`LandingFooter`로 일원화하거나, 반대로 layout에 두고 page에서 빼는 것 중 하나로 통일한다. **결정: layout에서 헤더/푸터 제거, page에서 렌더**(랜딩 단일 페이지라 page 조립이 명확). layout은 `.site-public` 래퍼 + 폰트 + `<main>{children}</main>`만 유지.

- [ ] **Step 4: 시안 `<style>` 규칙을 globals.css에 이식**

시안 `<style>`의 `.btn-rose/.btn-ghost/.chip/.card/.ph/.browser/details/.feat-ico/.wrap/.eyebrow/.display` 등 `.site-public` 하위 규칙을 `globals.css`에 `.site-public` 스코프로 추가(클래스명 충돌 없게 접두 또는 스코프 한정). 색 값은 Step B4의 변수 사용.

- [ ] **Step 5: 개발 서버로 시각 확인**

Run: `npm run dev` 후 `http://localhost:3100/` 접속(비로그인) → 랜딩 렌더 확인. 사전등록 폼 제출 시 BFF(8080) 필요 — api 워크트리 `./gradlew bootRun` 병행 또는 폼은 빌드/타입까지만 검증.

- [ ] **Step 6: 린트·빌드**

Run: `npm run lint && npm run build`
Expected: 성공(에셋 슬롯 플레이스홀더 상태로도 빌드 통과)

- [ ] **Step 7: 커밋**

```bash
git add src/components/public/landing-*.tsx src/components/public/waitlist-section.tsx "src/app/(public)/page.tsx" "src/app/(public)/layout.tsx" src/app/globals.css
git commit -m "feat(web): flori 제품 랜딩 섹션 구현(hero·사전등록·기능·FAQ·CTA·푸터)"
```

---

### Task B6: hazel 자산 제거 정리

**Files:**
- Delete: `src/components/public/{hero-section,statement-section,instagram-section,floating-cta,header,footer}.tsx`
- Delete: `src/lib/public-config.ts` (HAZEL_*)
- Modify: 위 파일을 import하던 모든 곳(grep로 확인)

- [ ] **Step 1: 참조 확인**

Run:
```bash
cd /Users/hansangho/Desktop/flori-ai/flori-web-session2
grep -rn "public-config\|HAZEL_\|hero-section\|statement-section\|instagram-section\|floating-cta\|components/public/header\|components/public/footer" src
```
Expected: `(public)/layout.tsx`·`page.tsx`·삭제 대상 컴포넌트 내부만 남아야 함(이미 B4/B5에서 교체됨). 그 외 참조가 있으면 해당 파일을 새 랜딩 컴포넌트로 교체.

- [ ] **Step 2: 삭제**

```bash
git rm src/components/public/hero-section.tsx src/components/public/statement-section.tsx src/components/public/instagram-section.tsx src/components/public/floating-cta.tsx src/components/public/header.tsx src/components/public/footer.tsx src/lib/public-config.ts
```
> `public-config.ts`를 다른 곳(예: 매니페스트·정책 페이지)에서 import하면 삭제 대신 flori 값으로 치환. grep 결과에 따라 판단.

- [ ] **Step 3: hazel 이미지 자산 정리(선택)**

`public/redesign/feed-*.jpg`, `public/instagram/*` 등 hazel 전용 이미지가 랜딩에서 더는 안 쓰이면 제거(선택 — 용량 정리). 다른 곳 참조 없을 때만.

- [ ] **Step 4: 린트·빌드·테스트**

Run: `npm run lint && npm run build && npm test`
Expected: 전부 green. 잔존 hazel import 에러 0.

- [ ] **Step 5: 커밋**

```bash
git add -A
git commit -m "chore(web): hazel 스토어프론트 컴포넌트·설정 제거"
```

---

### Task B7: 최종 검증

- [ ] **Step 1: web 전체 게이트**

Run: `cd /Users/hansangho/Desktop/flori-ai/flori-web-session2 && npm test && npm run lint && npm run build`
Expected: 모두 green.

- [ ] **Step 2: 수동 시나리오 점검(개발서버, api 병행)**

api: `cd /Users/hansangho/Desktop/flori-ai/flori-api-session2 && ./gradlew bootRun`
web: `npm run dev`
확인:
- 비로그인 `/` → 랜딩.
- 사전등록 폼 제출 → 카운트 +1, 중복 제출 시 에러 메시지, 카카오 단계 노출.
- `/policy/privacy`·`/policy/terms` 링크 이동, FAQ 토글 동작.
- (쿠키 시뮬레이션) 로그인 상태 `/` → `/admin` redirect — 실제 로그인 또는 쿠키 수동 주입으로 확인.

- [ ] **Step 3: api 전체 게이트**

Run: `cd /Users/hansangho/Desktop/flori-ai/flori-api-session2 && ./gradlew build`
Expected: BUILD SUCCESSFUL.

---

## 인프라 후속(코드 아님 — 사용자 안내)

PR 본문/별도 안내에 포함:
1. **DB 적용**: `docs/sql/migration/26-06-10-waitlist.sql`을 운영/스테이징 DB에 적용(사용자 승인 후).
2. **env 설정**: web `NEXT_PUBLIC_KAKAO_OPENCHAT_URL`(실링크), `NEXT_PUBLIC_SITE_URL=https://flori.ai.kr`.
3. **OAuth redirect URI**(kakao·google·naver) `admin.flori.ai.kr` → `flori.ai.kr` 갱신.
4. **쿠키 도메인 / CORS / CSP / 베이스 URL**을 `flori.ai.kr` 기준으로 점검(`lib/env.ts`·BFF CORS 설정).
5. **DNS / Vercel 도메인** 전환.

---

## Self-Review 메모

- 스펙 커버리지: 라우팅(B1)·랜딩 교체(B4/B5/B6)·사전등록 web(B2/B5)+api(A1~A4)·FAQ(B3)·정책 푸터(B5)·인프라(말미)·검증(B7) 모두 태스크 존재.
- 100명 상한: count 기반(약간 초과 허용 가능) — 스펙 §8 결정대로 단순 구현(엄격 락 미적용). `phone` UNIQUE로 중복만 엄격 차단.
- 미적용 항목(의도): 가격 카피·카카오 실링크·에셋 이미지는 플레이스홀더(스펙 §8 오픈이슈).
- 타입 일관성: web `WaitlistCount{count,capacity,closed}` ↔ api `WaitlistCountResponse/WaitlistRegisterResponse` 동일 필드. server action 필드 `shopName/phone` ↔ api `WaitlistRegisterRequest{shopName,phone}` 일치.
